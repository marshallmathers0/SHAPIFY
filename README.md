# SHAPIFY
SHAPIFY is an end-to-end XAI framework that explains black-box model predictions with SHAP and LIME, validates SHAP local accuracy, and compares attributions with model feature importance for reliable interpretation.
